$('div.questions').html("<%= j (render partial: 'current_questions', object: @active_category) %>")
$('.some-textarea').attr('id', '<%= @active_category.id %>')
$('.some-textarea').data("wysihtml5").editor.clear();